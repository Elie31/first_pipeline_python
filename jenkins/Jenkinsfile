pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                withPythonEnv('python3') {
                    sh 'python3 --version'
                }
            }
        }
    }
}