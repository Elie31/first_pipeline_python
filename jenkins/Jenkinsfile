pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                withPythonEnv('python3') {
                    sh 'pip3 install -r sources/requirements.txt'
                }
            }
        }
    }
}