pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                withPythonEnv('python') {
                    sh 'python --version'
                }
            }
        }
    }
}