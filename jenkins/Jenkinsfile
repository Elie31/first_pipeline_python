pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                withPythonEnv('/usr/bin/python3') {
                    sh 'pip install -r requirements.txt'
                }
            }
        }
    }
}